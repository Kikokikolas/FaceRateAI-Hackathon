VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key

# Leave these empty when the frontend and backend are served by the bundled Nginx proxy.
# Set a full URL only when API is hosted on another origin.
VITE_API_BASE_URL=
